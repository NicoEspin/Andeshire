import TextAgentsTable from "./TextAgents/TextAgentsTable";

type Props = {};

const TextAgentsView = (props: Props) => {
  return (
    <div>
      <TextAgentsTable />
    </div>
  );
};

export default TextAgentsView;
